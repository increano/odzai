---
description: 
globs: 
alwaysApply: false
---
The minimal-ui application is accessible with this command: 
 ```bash
cd packages/minimal-ui 
```

The minimal-ui ui is located here 
 ```bash
cd packages/minimal-ui/src/
```

Use the following command to run the minimal-ui :
```bash
cd packages/minimal-ui && ACTUAL_DATA_DIR=../../data yarn start
```

The minimal-ui-next is a copy of the minimal-ui we should keep the same features and functionality but a Next.js frontend

Use the following command to run the minimal-ui-next :
```bash
./start-services.sh
```

